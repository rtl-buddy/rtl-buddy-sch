// Minimal SV fixture for the Phase 1 vertical slice: a single module
// with no ports, parameters, or instances. The renderer should print
// just the module name on one line.
module empty;
endmodule
