.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
*.swp
.DS_Store
# Vendored Verible binary fetched by the setup flow.
vendor/verible/
# Staged SPA bundle written by scripts/prebuild_viewer.py before
# `uv build`. Lives under the package tree so uv_build's default
# discovery picks it up, but never committed — it's a build artefact.
src/rtl_buddy_view/_viewer_bundle/
